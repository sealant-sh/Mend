---
"@sealant/mend": minor
---

Install only the CLI through npm or the POSIX bootstrap. Add explicit Docker server setup with exact
version pins, private configuration generations, persistent identity, daemon volume ownership
checks, and a real Engine registry roundtrip. Validate release assets and image versions before
activation. Support explicit private origins, configurable ports, local release assets, and offline
setup.
