# Convenience entry point for the versioned bundle contract used by `mend setup`.
# Put the generated .env beside this file, then run `docker compose up -d --wait`.
include:
  - path: deploy/docker/compose.v1.yaml
