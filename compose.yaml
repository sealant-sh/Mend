# Convenience entry point for the same contract used by `mend server setup`.
# Direct use: docker compose --env-file ~/.config/mend/server.env up -d --wait
name: mend
include:
  - path: deploy/docker/compose.v1.yaml
